use crate::structure::node::Node;
use std::collections::HashMap;

pub struct Graph {
    pub nodes: HashMap<i64, Node>,

}

impl Graph {
    pub fn new(nodes: Vec<isize>, edges: Vec<(isize, isize)>) -> Graph {
        
    }
}
